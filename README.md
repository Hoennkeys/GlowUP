# VendaPro CRM

CRM de vendas para pequenos negócios — painel, funil, chats, e-mails, agenda e propostas comerciais.

## Stack

- React 19 + TypeScript
- TanStack Start / Router / Query
- Tailwind CSS 4 + shadcn/ui
- Vite 7

## Desenvolvimento local

```bash
npm install --legacy-peer-deps
npm run dev
```

Abra [http://localhost:8080](http://localhost:8080) (ou a porta indicada no terminal).

## Scripts

| Comando | Descrição |
|---------|-----------|
| `npm run dev` | Servidor de desenvolvimento |
| `npm run build` | Build de produção |
| `npm run preview` | Preview do build |
| `npm run lint` | ESLint |

## Estado atual

Snapshot inicial do projeto (dados mock + persistência em `localStorage`) antes das correções de bugs identificadas no mastercheck.
